name: Build 小说创作器
on: push
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: 安装Qt6
        uses: jurplel/install-qt-action@v4
        with:
          version: 6.6.2
          host: windows
          target: desktop

      - name: CMake编译
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --config Release

      - name: 打包成单文件（解决缺少dll报错）
        run: |
          windeployqt build/Release/NovelWriter.exe

      - name: 上传完整打包文件
        uses: actions/upload-artifact@v4
        with:
          name: 小说创作器_完整单文件版
          path: build/Release/
